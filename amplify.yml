version: 1
backend:
  phases:
    build:
      commands:
        - npm ci --prefer-offline
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
frontend:
  phases:
    preBuild:
      commands:
        - echo "Skipping npm ci - packages already installed in backend phase"
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
test:
  phases:
    preTest:
      commands:
        - npm run build
    test:
      commands:
        - npx start-server-and-test start http://localhost:3000 cypress:run
    postTest:
      commands:
        - npx mochawesome-merge cypress/report/*.json > cypress/report/combined.json
        - npx marge cypress/report/combined.json -f report -o cypress/report
  artifacts:
    baseDirectory: cypress
    configFilePath: "**/mochawesome.json"
    files:
      - "**/*.png"
      - "**/*.mp4"